--- a/cli/tools/delegate.py
+++ b/cli/tools/delegate.py
@@ -280,12 +280,14 @@
-def _run_claude(task, context, cwd=None, timeout=90, idle_timeout=30):
-    prompt = f"Context:\n{context}\n\nTask:\n{task}" if context else task
-    cmd = [exe, "-p", prompt, "--output-format", "text"]
+def _run_claude(prompt, model="", cwd=None, timeout=90):
+    cmd = [exe, "-p", "--output-format", "json", "--no-session-persistence"]
+    if model:
+        cmd += ["--model", model]
     proc = subprocess.Popen(
         harden_win_argv(cmd),
         stdout=subprocess.PIPE, stderr=subprocess.PIPE,
-        stdin=subprocess.DEVNULL,
+        stdin=subprocess.PIPE,
         text=True, encoding="utf-8", errors="replace", cwd=cwd,
     )
-    output, err, status = _communicate_with_heartbeat(proc, timeout=timeout, idle_timeout=idle_timeout)
+    output, err, status = _communicate_with_heartbeat(proc, timeout=timeout, idle_timeout=0,
+                                                      stdin_text=prompt)
